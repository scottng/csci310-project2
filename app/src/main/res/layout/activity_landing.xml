<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".LandingActivity"
    style="@style/AppTheme"
    android:background="@color/colorPrimaryDark">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:id="@+id/text_welcome"
            android:layout_margin="24dp"
            android:text="@string/landing_text_welcome"
            style="@style/Heading1"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <Button
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:id="@+id/button_create_account"
            style="@style/LongButton"
            android:text="@string/landing_button_create_account"
            app:layout_constraintStart_toStartOf="@id/text_welcome"
            app:layout_constraintEnd_toEndOf="@id/text_welcome"
            app:layout_constraintTop_toBottomOf="@id/text_welcome"
            android:layout_marginTop="16dp"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>


    <TextView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:id="@+id/text_login"
        style="@style/Body"
        android:text="@string/landing_text_login"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_margin="24dp" />

    <Button
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:id="@+id/button_directto_login"
        android:text="@string/landing_button_login"
        app:layout_constraintStart_toEndOf="@id/text_login"
        app:layout_constraintBaseline_toBaselineOf="@id/text_login"
        android:layout_marginLeft="4dp"
        android:textColor="@color/colorAction"
        android:background="@null"
        android:textAllCaps="false"
        />

</androidx.constraintlayout.widget.ConstraintLayout>